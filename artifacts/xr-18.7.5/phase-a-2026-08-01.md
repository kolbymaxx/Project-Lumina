# Phase A capture — 2026-08-01

Source: live XR ramdisk SSH after usbliter8 → hsbugss xr-ramdisk boot.

## Summary
- UDID: `00008020-00117540340B002E`
- Ramdisk: iOS **15.1** restore (`19B5042h`), root `/dev/md0` HFS **RO**
- System: `/dev/disk0s1s1` → `/mnt1` OK  
  `/mnt1` SystemVersion = **iOS 18.7.5 (22H311)** (real device OS)
- Data: `/dev/disk0s1s2` → FAIL `mount_apfs: Program version wrong`
- Preboot-ish: `/dev/disk0s1s5` → `/mnt4` OK (`mobile/Library` present)
- Kernel under `/mnt1`: not located yet (`Caches` / `Kernels` empty or missing)

## Blocker
15.1 `mount_apfs` cannot mount iOS 18 Data volume. Need a newer restore
ramdisk / mount tooling, or another Data access path.

## Follow-ups
1. Host-extract kernelcache from `iPhone11,8_18.7.5_22H311_Restore`
2. Deeper search under `/mnt1` and `/mnt4` for kernel / cryptex / preboot
3. Evaluate newer n841 restore ramdisk that ships a newer `mount_apfs`
